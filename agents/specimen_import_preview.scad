// Reference-only preview of the original STL specimen.
// Render with OpenSCAD to inspect the imported mesh while authoring the
// parametric approximation in ../e210_hatchback_trunk_floor_handle.scad.

import("../specimen/corolla+hatchback+trunk+floor+handle.stl", convexity = 10);
